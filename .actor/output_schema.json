{
    "actorOutputSchemaVersion": 1,
    "title": "Real Estate & Property Intelligence Output",
    "properties": {
        "dataset_view": {
            "type": "string",
            "title": "🏠 Property Data (News & Pricing)",
            "description": "View the scraped property news, AI insights, and listing data in a table.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=intelligence"
        },
        "property_report": {
            "type": "string",
            "title": "📑 Daily Property Report (JSON)",
            "description": "If run in 'PROPERTY_REPORT' mode, this link returns the consolidated daily analysis.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=json&desc=1&limit=1"
        },
        "full_json": {
            "type": "string",
            "title": "🔗 Full Dataset (JSON API)",
            "description": "Programmatic access to all data generated by this actor.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=json"
        }
    }
}